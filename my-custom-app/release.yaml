# apiVersion: helm.toolkit.fluxcd.io/v2
# kind: HelmRelease
# metadata:
#   name: app         # LEAVE AS IS (Auto-Renamed)
#   namespace: default # LEAVE AS IS (Auto-Renamed)
# spec:
#   interval: 5m
#   chart:
#     spec:
#       # 1. Point to the specific folder inside your Repo
#       chart: ./apps/web-servers/my-custom-app/charts
      
#       # 2. Point to the Git Repo itself (instead of Bitnami)
#       sourceRef:
#         kind: GitRepository
#         name: flux-system
#         namespace: flux-system
        
#       # 3. Use the Chart.yaml version or "*"
#       version: "*" 
      
#   values:
#     # Your custom values for the local chart
#     color: blue
#     replicaCount: 1
